machine:

  node:
    version:
      6.5.0

dependencies:
  pre:
    - npm install -g grunt-cli # install from a different package manager
    - npm --version # use a custom version of bundler
    - npm install
  post:
    - rm -r home/ubuntu/.m2, home/ubuntu/.go_workspace, home/ubuntu/.cache/bower, home/ubuntu/syd_17s1_orange/node_modules
test:
  override:
    - npm run test
